apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app-volunteer-service
    {{- include "solidarytech.labels" .| nindent 4 }}
  name: app-volunteer-service
  namespace: {{ (index .Values "volunteer-service").namespace }}
spec:
  strategy:
    type: Recreate
  replicas: {{ (index .Values "volunteer-service").replicas}}
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: app-volunteer-service
  template:
    metadata:
      labels:
        app: app-volunteer-service
    spec:
      containers:
        - name: app-volunteer-service
          image: {{ (index .Values "volunteer-service").image }}
          imagePullPolicy: {{ (index .Values "volunteer-service").imagePullPolicy }}
          env:
            - name: POSTGRES_DB
              value: {{ (index .Values "volunteer-service").env_vars }}
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          startupProbe:
           httpGet:
             path: /health
             port: {{ (index .Values "volunteer-service").port }}
           failureThreshold: 60
           periodSeconds: 5
           timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ (index .Values "volunteer-service").port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: {{ (index .Values "volunteer-service").port }}
            initialDelaySeconds: 180
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          envFrom:
            {{- toYaml (index .Values "volunteer-service").envFrom | nindent 12 }}
